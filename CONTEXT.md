# dsh-lark

DSH 的 IM 遥控器：把一个飞书/Lark 聊天接到一个 DSH agent 会话上，让人在聊天里派任务、看过程、做决定。它不是通用飞书 bot——聊天是遥控器，agent 才是干活的那个。

## Language

### 会话与位置

**会话（Conversation）**：
聊天里被当作同一个上下文来对待的那一撮消息，由 `sessionScope` 决定它是整个聊天、一个话题，还是某个人。
_Avoid_: 聊天、chat、群

**评论会话（Comment Conversation）**：
由一篇云文档的评论区派生的会话。它的身份是那篇文档，不是某个聊天——所以同一篇文档上的每条评论线程共享一个上下文，线程只决定回复落在哪里。它与聊天派生的会话是同一种东西的两个入口，不是两套机制。
_Avoid_: 评论线程、doc session、第二会话面

**工作区（Workspace）**：
一个会话当前所在的目录。会话的身份由「会话 × 工作区」共同决定，所以换目录等于换到另一个会话。
_Avoid_: 目录、cwd、项目

### 入站与出站

**入站文件（Inbound File）**：
渠道从外部取回、落进工作区供 agent 读取的不可信数据。它按**方向与信任**定义，不按来源形式：一条文件消息、一张图片，都是入站文件——差别只在渠道要走哪条路把它取回来。图片额外还能作为模型可见的内容块附上去，但它落盘这件事与模型看不看得见无关。云文档不在其列：渠道不代取正文，agent 自己去读，所以那份内容是 agent 取回的快照，不是入站文件。
_Avoid_: 附件、上传、attachment

**收件箱（Inbox）**：
工作区里承载入站文件的那个地方。按消息分组，只增不删——清理是人的决定，渠道不替他做。
_Avoid_: 缓存、临时目录、下载目录

**快照（Snapshot）**：
一份从源头取回的内容在**被取回那一刻**的样子，而不是它此刻在源头的样子。文档会被人继续改，盘上那份不会跟着变。取回它的是 agent 而不是渠道，但这个区别仍要一直说出来，因为"agent 读到的和我现在看到的不是同一版"是这条路上最容易被忽略的失败。
_Avoid_: 副本、缓存、最新版

**产物（Artifact）**：
agent 在工作区里生成、要送回聊天的东西——报告、diff、图表。送回的形态可以是一条文件消息，也可以是一篇飞书文档，那不改变它是什么。它与入站文件的区别不在格式，而在方向和信任：入站文件不可信，产物是 agent 自己写的。
_Avoid_: 结果、输出、附件

**注记（Note）**：
渠道替某个它没能交给模型的东西留下的一行说明。存在的理由是：一个收到截图却当作没收到的模型，比一个知道自己没收到的模型更糟。
_Avoid_: 提示、warning、占位符

### 协作套件

**能力（Capability）**：
一项要飞书授权才点得亮的渠道功能。能力与权限不是一回事：权限是租户批给应用的 scope，能力是渠道据此决定要不要把某个工具摆到模型面前。没点亮的能力必须如实告诉模型它不在，而不是让模型去调一个注定失败的工具。
_Avoid_: 权限、scope、feature、开关

**锚点（Anchor）**：
一条评论挂在文档里的位置，也就是一个 block id。没有锚点的评论是全文评论——它出现在文档右侧，但不指向任何一段话。
_Avoid_: 位置、段落、block、selection
